# AGENTS.md

## 目标
在 Antigravity、Cursor、VS Code/Codex 三端切换时，保持同一项目上下文、同一执行标准、同一交接格式，避免“上一轮做了什么”丢失。

## 作用范围
- 本文件作用于仓库根目录及全部子目录。
- 若子目录另有更细规则，子目录规则优先；否则以本文件为准。

## 会话启动顺序（强制）
每次新模型接手时，按顺序读取并确认：
{{startupReadOrderBullets}}

## 默认读取粒度（强制）
为控制 token 消耗，默认执行“摘要优先、按需深读”，除非当前任务明确需要全文核对：
1. `AGENTS.md`：全文读取。
2. `task_registry.md`：只读取当前活跃 `Task-ID` 对应行。
3. `progress.md`：优先读取顶部 `Latest Handoff Snapshot`。
4. `task_plan.md`：优先读取当前任务相关段、最新计划、未完成项与风险。
5. `findings.md`：优先读取与当前 `Task-ID` 相关的最近事实和待确认问题。
6. `修改记录_会话备忘.md`：优先读取最近 1-3 条与当前 `Task-ID` 相关的 `Session Record`。
7. 其他长文件：仅在当前任务需要时读取相关段落，不默认全文通读。

## 全文读取触发条件（强制）
只有在以下情况才要求全文或大范围补读：
1. 用户明确要求“全文核对 / 全部阅读 / 通读上下文”。
2. 仅靠快照和近期记录无法恢复当前 `Task-ID`。
3. 多个交接文件对同一任务描述冲突，需要回溯溯源。
4. 正在修改规则文件、接力模板、任务协议或做高风险回归排查。

## 常用中文话术（建议）
以后优先直接用中文自然语言触发，不必记英文 skill 名：
1. `接手这个项目，按接力开发规则来`
2. `给这个项目初始化接力开发`
3. `重新同步当前任务`
4. `做一次标准交接`
5. `按接力开发流程处理这个项目`

## 启动检查（强制）
接手后先执行并回报关键结果：
1. `Get-Location`
2. `Get-ChildItem -Force`
3. `if (Test-Path .git) { git status --short } else { 'NO_GIT_REPO' }`
4. `python -V`

## 会话结束交接（强制）
结束前必须更新 `progress.md` 顶部“Latest Handoff Snapshot”，至少包含：
1. Task-ID / Task-Name
2. 本轮修改文件列表
3. 已完成事项
4. 未完成事项
5. 风险/阻塞
6. 下一步第一条命令（可直接复制执行）

## 多会话防混淆（Task-ID，强制）
1. 每个会话开场先声明并锁定一个 `Task-ID`（示例：`T20260227-01`）和 `Task-Name`。
2. 所有输出（Session Context、Latest Handoff Snapshot、Session Record）必须包含同一个 `Task-ID`。
3. 同一会话只处理一个 `Task-ID`；若出现其他任务上下文，标记为 `IGNORE`，不执行跨任务步骤。
4. 切换到另一个问题前，先在 `task_registry.md` 更新当前任务状态，再启动新 `Task-ID`。
5. 结束会话前，必须回写 `task_registry.md` 的当前任务行（状态、最后更新时间、Next First Command）。

## 项目事实（可按需调整）
1. 数据目录：`{{profile.dataDir}}`
2. 语义模型目录：`{{profile.modelDir}}`
3. 主构建脚本：`{{profile.builderScript}}`
4. 查询服务：`{{profile.queryScript}}`
5. 主输出文件：`{{profile.outputFile}}`

## 硬约束
1. 所有回复、思考和任务清单使用 `{{profile.language}}`。
2. 对外展示股票代码统一 {{profile.stockCodeDigits}} 位（不足前补 0）。
3. 未执行验证命令前，不得声称“已完成/已修复”。
4. 不得将数据目录写回旧路径 `{{profile.legacyDataDir}}`。
5. 任何错误必须写入 `task_plan.md` 的错误日志或 `progress.md` 的错误记录。
6. 同一会话严禁混写多个 `Task-ID` 的 TODO、风险和结论。

## 三端对齐约定
1. Antigravity：全局规则可放 `C:\Users\Administrator\.gemini\GEMINI.md`；项目级以本 `AGENTS.md` 为主。
2. Cursor：项目规则放 `.cursor/rules/*.mdc`；`.cursorrules` 仅保留兼容入口。
3. VS Code/Copilot：仓库规则放 `.github/copilot-instructions.md`，内容从本文件派生。

## 交接快照模板

```markdown
## Latest Handoff Snapshot
- Timestamp: YYYY-MM-DD HH:mm
- Task-ID:
- Task-Name:
- Active Goal:
- Completed This Session:
- Files Changed:
- Open TODO:
- Risks/Blockers:
- Next First Command:
```
